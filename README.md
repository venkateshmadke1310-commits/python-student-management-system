# python-student-management-system
A console-based Student Management System built using Python. It supports full CRUD operations (Add, View, Search, Update, Delete) with file handling for data persistence.
